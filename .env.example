# GitHub Personal Access Token (recommended for higher rate limits)
# Generate at: https://github.com/settings/tokens
# No special permissions needed - public repo access only
GITHUB_TOKEN=your-github-token

# SDK Auto-Update Configuration
# Update check interval: aggressive (1h), moderate (6h), conservative (24h - default), weekly (7d)
SDK_UPDATE_INTERVAL=conservative

# Enable/disable automatic SDK updates
# Set to "false" to disable auto-updates
SDK_AUTO_UPDATE=true
